# Multilingual comparison

Visit comparison operates on canonical snapshot data, never translated strings. Localized labels may describe `changed`, `unchanged`, `new`, `resolved`, `unknown`, or `conflict`, but the underlying comparison result remains stable across display languages.

Original evidence remains available for clinical review. Translation failure therefore affects presentation, not comparison computation.
