# Multilingual verification

Doctor verification always exposes the original source and normalized fact independently. A display translation is an aid, not evidence. Verify, correct, reject, uncertain, confirm-current, and keep-previous actions retain the immutable audit trail introduced in Phase 10.

Doctor preference may be persisted through the signed doctor language endpoint. Authorization and idempotency controls are unchanged.
